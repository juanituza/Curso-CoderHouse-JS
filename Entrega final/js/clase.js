class Clase{
    static contadorClase=0;

    constructor(nombre,entrenadores,alumno){
        this._nombre = nombre;
        this._entrenadores= entrenadores=[];
        super(alumno);
    }

    asignarClase(){
        return 
    }

    /* const personas = ["pepito", "pepita", "mariano", "camila", "ester", "oscar"];

const eliminar = (nombre) => {
  let index = personas.indexOf(nombre);

  if(index != -1){
    personas.splice(index, 1);

    console.log(personas);
  }
};

eliminar("mariano"); */

}